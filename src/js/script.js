//
// let order = `up`;
//
// // const sortByTons = (a, b) => {  //sorteren van tons
// //   if(order === `up`){ //als de tonheader op up staat van klein naar groot
// //     return parseInt(a.Tons, 10) - parseInt(b.Tons, 10);
// //   }else{ //anders van groot naar klein
// //     return parseInt(b.Tons, 10) - parseInt(a.Tons, 10);
// //   }
// // };
//
// const handleClickSubNav = e => {
//   order = (order === `up`) ? `down` : `up`;
//   e.currentTarget.dataset.sort = order;
//   // updateData(originalSubs);
// };
//
// const init = () => {
//   document.querySelector(`.navActiviteiten`).addEventListener(`click`, handleClickSubNav);
// };
//
// init();
